library(tidyverse)
#library(ggplot2)

var <- factorial(3)

vec <- c(1,2,3)

v_list <- list(Nums = 1:5,"apple",c(TRUE,FALSE))
v_list$Nums

mpg

ggplot(data=mpg) +
  geom_point(mapping = aes(x = displ, y=hwy))


